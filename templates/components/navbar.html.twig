<nav class="navbar navbar-expand-lg navbar-light bg-dark border-bottom">
	<div class="container-fluid">
		<div id="sidebarToggle"><i  style='color:white;' class="fas fa-toggle-on fa-lg"></i></div>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav ms-auto mt-2 mt-lg-0">
				{% if app.user == null %}
					<li class="nav-item mx-1">
						<a href="{{path('app_login')}}" type="button" class="btn btn-primary">Login</a>
					</li>
					<li class="nav-item mx-1">
						<a href="{{path('app_register')}}" type="button" class="btn btn-secondary">Register</a>
					</li>
				{% else %}
					<li class="nav-item mx-1">
						<a href="{{path('app_logout')}}" type="button" class="btn btn-danger">Logout</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
